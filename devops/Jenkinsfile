pipeline {
  agent any

  stages {
    // EJERCICIO 1: Estructura básica e Información
    stage('Info') {
      steps {
        echo "Job: ${env.JOB_NAME}"
        echo "Build: ${env.BUILD_NUMBER}"
        echo "Pipeline de Java inicializado correctamente"
      }
    }
  }
}